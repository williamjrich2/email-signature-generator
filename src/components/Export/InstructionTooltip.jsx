export default function InstructionTooltip() {
  return (
    <div className="rounded-2xl border border-white/10 bg-white/5 px-4 py-3 text-sm text-app-muted">
      Open Outlook or Gmail settings, locate the signature editor, then paste the
      rich clipboard version. Use the raw HTML export when you need to inspect or
      host the markup manually.
    </div>
  )
}
